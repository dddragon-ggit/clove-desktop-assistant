# 工作进度

## 项目

- 名称：桌面操作助手
- 工作目录：`D:\Cursor_project\4_interesting`
- 当前环境：`D:\anaconda3\envs\app\python.exe`

## 交接摘要

- 当前代码状态：`可运行`
- 当前主语言：`Python`
- 当前主技术路线：`Python + SQLite + fake/real 双模型适配器`
- 当前默认存储：`SQLite`
- 当前默认运行入口：`python -m desktop_assistant`
- 当前 UI 状态：`尚未开始正式开发`

当前已经完成：

- Python 工程骨架
- 工作流编排器
- 规则引擎
- fake planner / reviewer / executor / context provider
- SQLite 持久化
- 提供方配置读取与本地加密保存
- OpenAI-compatible `responses` 真实适配器
- 基础单元测试

当前还没完成：

- 悬浮助手 UI
- 真实模型结果的稳定联调确认
- 真实 Planner / Reviewer 输出质量验证
- 重试 / 降级 / 回退机制

## 当前结论

- 本地工程骨架已经可跑
- `fake` 模式完整可用
- SQLite 已接入主运行路径
- 真实模型适配器代码已完成
- 中转站联通性已确认存在
- 真实链路尚不能视为 fully verified

## 已确认决策

- 技术栈已经从 `Electron + TypeScript` 改为 `Python` 路线
- 当前桌面框架目标是：`PySide6`
- 当前核心实现优先顺序是：`可调试性 > 解耦 > 可运行 > UI`
- 首版主要交互入口应收敛到悬浮助手
- 首版 `restore_workspace` 只做“打开相关资源”，不做窗口布局恢复
- 主运行路径使用本地 SQLite，而不是仅内存存储
- 真实模型使用 `OpenAI-compatible responses API`

## 当前可运行能力

### 1. fake 工作流链路

已跑通：

- 请求输入
- 上下文生成
- planner
- policy
- reviewer
- dry run 输出
- SQLite 持久化

### 2. real 适配器代码链路

已完成：

- provider config 读取
- `api_key` 本地加密保存
- `responses` API 请求封装
- planner / reviewer 真实适配器
- fake / real 运行切换

尚未完全确认：

- 真实结构化输出稳定性
- 当前中转站对复杂 `responses` 请求体的稳定兼容性

## 当前文件结构重点

### 规格与记录

- 规格文档：[desktop-assistant-spec.md](</d:/Cursor_project/4_interesting/docs/desktop-assistant-spec.md>)
- 交接记录：[工作进度.md](</d:/Cursor_project/4_interesting/工作进度.md>)

### 核心代码

- 运行入口：[demo.py](</d:/Cursor_project/4_interesting/desktop_assistant/demo.py>)
- 数据模型：[models.py](</d:/Cursor_project/4_interesting/desktop_assistant/models.py>)
- 模块接口：[protocols.py](</d:/Cursor_project/4_interesting/desktop_assistant/protocols.py>)
- 编排器：[orchestrator.py](</d:/Cursor_project/4_interesting/desktop_assistant/core/orchestrator.py>)
- 规则引擎：[policy.py](</d:/Cursor_project/4_interesting/desktop_assistant/core/policy.py>)

### 适配器

- fake 适配器：[fake.py](</d:/Cursor_project/4_interesting/desktop_assistant/adapters/fake.py>)
- real 适配器：[openai_responses.py](</d:/Cursor_project/4_interesting/desktop_assistant/adapters/openai_responses.py>)
- provider config：[config.py](</d:/Cursor_project/4_interesting/desktop_assistant/config.py>)

### 存储

- 内存存储：[in_memory.py](</d:/Cursor_project/4_interesting/desktop_assistant/storage/in_memory.py>)
- SQLite 存储：[sqlite.py](</d:/Cursor_project/4_interesting/desktop_assistant/storage/sqlite.py>)

### 提示词

- planner prompt：[planner_system.txt](</d:/Cursor_project/4_interesting/desktop_assistant/prompts/planner_system.txt>)
- reviewer prompt：[reviewer_system.txt](</d:/Cursor_project/4_interesting/desktop_assistant/prompts/reviewer_system.txt>)

### 测试

- 工作流测试：[test_orchestrator.py](</d:/Cursor_project/4_interesting/tests/test_orchestrator.py>)
- SQLite 测试：[test_sqlite_storage.py](</d:/Cursor_project/4_interesting/tests/test_sqlite_storage.py>)
- provider config 测试：[test_provider_config.py](</d:/Cursor_project/4_interesting/tests/test_provider_config.py>)
- real adapter 测试：[test_openai_responses.py](</d:/Cursor_project/4_interesting/tests/test_openai_responses.py>)

## 当前可验证命令

### 跑全部测试

```powershell
D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v
```

当前已知结果：

- `8/8` 测试通过

### 跑 fake + SQLite dry run

```powershell
D:\anaconda3\envs\app\python.exe -m desktop_assistant --mode dry_run --request 开始做周报 --storage sqlite --ai-backend fake
```

预期：

- 输出 JSON
- `status = dry_run_ready`
- 会写入本地 SQLite

### 跑 real + SQLite dry run

```powershell
D:\anaconda3\envs\app\python.exe -m desktop_assistant --mode dry_run --request 开始做周报 --storage sqlite --ai-backend real
```

说明：

- 该命令依赖可用的 provider config
- 当前命令可能因中转站暂时不稳定而失败

## 本地数据与配置路径

- SQLite 数据库：[desktop_assistant.db](</d:/Cursor_project/4_interesting/runtime/data/desktop_assistant.db>)
- provider config 文件：[model_provider.json](</d:/Cursor_project/4_interesting/runtime/data/model_provider.json>)

注意：

- `model_provider.json` 中保存的是加密后的 `api_key`
- 不要把完整 `api_key` 写进日志、测试输出或交接文档

## 已验证内容

- `app` conda 环境中的 Python 可正常使用
- Python 工程骨架可导入、可运行
- fake planner / reviewer / executor 链路正常
- SQLite 持久化正常
- Windows 下 SQLite 文件锁问题已修复
- 真实 provider config 保存 / 读取逻辑正常
- 真实 `responses` 适配器的本地单测正常
- 至少一次真实请求已成功打到中转站

## 未验证内容

以下项目前**不能**视为已完全通过：

- 真实 `Planner` 是否稳定返回合法 `PlannerResult`
- 真实 `Reviewer` 是否稳定返回合法 `ReviewResult`
- `json_schema` 结构化输出在当前中转站上的稳定兼容性
- `reasoning.effort` 在当前中转站上的稳定兼容性
- 提示词在真实模型上的实际效果
- 复杂请求下的真实返回质量
- `502 / 429 / timeout / 非 JSON` 下的真实回退体验

## 当前阻塞与已知问题

- 当前真实联调的主要阻塞不是“完全连不上中转站”，而是中转站**偶发不稳定**
- 已出现过 `502 Bad Gateway`
- 用户已明确说明：`502` 可视为中转站暂时不稳定，不等于本地适配器错误
- 当前尚未完成真实链路的 retry / fallback / degrade 机制

## 建议下一步

优先建议：

1. 增加 real adapter 的 `重试 + 降级 + 回退到 fake` 机制
2. 补“真实联调待验证清单”的执行脚本或测试脚本
3. 在此基础上再开始悬浮助手 UI 骨架

如果后续由别的 AI 接手，建议先做：

1. 运行全量测试，确认仍为 `8/8`
2. 跑一次 `fake + sqlite` dry run，确认基础链路无回归
3. 再尝试 `real + sqlite`，记录中转站实际响应
4. 只有在真实模型联调稳定后，再推进 UI 与交互整合

## 进度记录

### 2026-04-26 14:20

- 已确认当前工作目录结构，仅存在 `docs/desktop-assistant-spec.md`
- 已验证 `app` 环境可用，当前解释器为 `Python 3.12.12`
- 已开始进入工程搭建阶段

### 2026-04-26 14:28

- 已将规格文档中的技术栈统一为 Python 方案
- 已确认首版工程方向为 `Python + PySide6 + SQLite`
- 已补齐文档中的 Python 运行结构、模块解耦与可调试要求

### 2026-04-26 14:41

- 已创建 Python 工程骨架 `desktop_assistant/`
- 已落下核心模块：`models`、`protocols`、`policy`、`orchestrator`
- 已实现首批假适配器：`FakePlanner`、`FakeReviewer`、`FakeExecutor`、`FakeContextProvider`
- 已实现 `InMemoryStorage`
- 已提供可运行入口 `python -m desktop_assistant`
- 已补充基础单元测试 `tests/test_orchestrator.py`

### 2026-04-26 14:46

- 已在 `app` 环境中运行 `python -m unittest discover -s tests -v`
- 当前 3 个基础测试全部通过
- 已运行 `python -m desktop_assistant --mode dry_run --request 开始做周报`
- 已验证当前骨架可完成：上下文获取 -> 规划 -> 规则判断 -> 审查 -> dry run 输出

### 2026-04-26 14:52

- 已由用户手动验证 dry run 输出
- 当前确认 `FakePlanner -> PolicyEngine -> FakeReviewer -> JSON 输出` 链路工作正常

### 2026-04-26 15:05

- 已新增 `SQLiteStorage`
- 当前主运行入口默认使用 SQLite 持久化
- 数据库默认路径为 `runtime/data/desktop_assistant.db`
- 已补充 SQLite 相关测试 `tests/test_sqlite_storage.py`
- 已修复 Windows 下 SQLite 连接未及时关闭导致的文件锁问题

### 2026-04-26 15:07

- 已在 `app` 环境中重新运行完整测试
- 当前测试结果：`5/5` 通过
- 已验证 SQLite 模式下的 dry run 输出正常
- 已确认数据库文件已成功写入 `runtime/data/desktop_assistant.db`

### 2026-04-26 15:22

- 已新增真实云端模型适配器：
  - `desktop_assistant/config.py`
  - `desktop_assistant/adapters/openai_responses.py`
  - `desktop_assistant/prompts/planner_system.txt`
  - `desktop_assistant/prompts/reviewer_system.txt`
- 已支持 `fake / real` 双后端切换
- 已支持本地提供方配置文件与 `api_key` 加密保存
- 已支持运行时环境变量优先加载提供方配置

### 2026-04-26 15:25

- 已新增真实适配器相关测试：
  - `tests/test_openai_responses.py`
  - `tests/test_provider_config.py`
- 当前完整测试结果：`8/8` 通过

### 2026-04-26 15:28

- 已使用用户提供的中转配置完成本地提供方配置初始化
- 已确认本地可成功发起到中转站的真实网络请求
- 当前真实链路未完全跑通的唯一已知阻塞为中转站偶发 `502 Bad Gateway`
- 根据用户确认，可将该问题视为中转站暂时不稳定，而非本地适配器无法连通

### 2026-04-26 15:31

- 已明确以下内容目前**尚未完成真实服务端验证**，不能视为已完全测试通过：
  - `Planner` 是否能稳定返回合法 `PlannerResult`
  - `Reviewer` 是否能稳定返回合法 `ReviewResult`
  - `json_schema` 结构化输出能力在当前中转站上的真实兼容性
  - `reasoning.effort` 等高级字段在当前中转站上的真实兼容性
  - 提示词在真实模型上的实际效果
  - 复杂请求组合下的真实返回质量
  - `502 / 429 / timeout / 非 JSON` 等错误场景下的真实降级表现
- 当前这些项应归类为：`待真实联调验证`
## 2026-04-30

- 新增 `desktop_assistant/activity/` 后端模块，用于以非侵入方式感知用户当前活动：前台应用、窗口标题、可能打开的文件、所属项目和最近活动元数据。
- 新增活动模块结构：`models.py`、`resolver.py`、`sampler.py`、`recent_files.py`、`process.py`、`store.py`、`__init__.py`。
- 活动感知隐私边界已明确：不监听键盘、不截图、不 OCR、不读取剪贴板、不读取文件正文、不读取网页或聊天内容，仅保存应用/窗口/文件/项目元数据。
- 新增 `tests/test_activity.py`，覆盖命令行文件识别、窗口标题与最近文件匹配、项目归因、活动日志去重和 sampler 组合链路。
- 更新 `docs/desktop-assistant-spec.md`，加入 `activity/` 模块职责、隐私边界、文件结构和 `activity_snapshots` 数据项。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `165/165` tests OK。
- 根据新的首页助手方案，已先更新 `docs/desktop-assistant-spec.md`：新增 `todo/`、`habits/`、`workspace/`、`input_router/` 后端契约，并明确首页方框/小球、待办颜色、行为预测、工作区确认流与单文件不超过 400 行的维护要求。
- 新增 `desktop_assistant/todo/`：真实待办模型、JSON 存储、完成/删除/更新、重要事项计数、首页问候语和红/橙/黄/绿紧急度计算。
- 新增 `desktop_assistant/habits/`：基于 `activity` 的每日行为 Markdown 记录、最近 14 天清理、`next_action_prediction.json` 预测文件和 `HabitTracker.capture_once()`。
- 新增 `desktop_assistant/workspace/`：把待办、用户目标或当前活动转换为工作区建议，并输出可交给现有 policy/reviewer/action-level trust 的 `ActionPlan`。
- 新增 `desktop_assistant/input_router/`：首页底部输入框路由，支持待办、工作区、继续刚才工作、普通对话，以及 `Tab` 接受灰色预测。
- 新增测试：`tests/test_todo.py`、`tests/test_habits.py`、`tests/test_workspace_input_router.py`，覆盖 11 个新增后端契约用例。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `176/176` tests OK。
- 已检查单文件行数，当前最大 Python 文件为 `desktop_assistant/ui/main_window_interactions.py`，约 `298` 行，符合单个文件不大于 400 行的维护要求。
- 开始继续补齐非 UI 后端闭环：`show_tasks/create_reminder` 接真实 To-do、To-do 推迟/取消/执行记录、`ui_state/` 持久化、`activity` 隐私开关、`habits` 定时采样、`workspace` refine/保存配方、`confirmation/` 前端确认流 API。
- 已完成非 UI 后端闭环补齐：
  - `show_tasks` / `create_reminder` 已接入真实 `TodoStore`，并更新 capability 默认 handler。
  - `TodoStore` 已支持推迟、取消、执行记录、工作区确认状态和 trusted action keys。
  - 新增 `ui_state/`，持久化方框/小球位置、大小、模式、隐身、透明度、模糊偏好。
  - 新增 `activity/privacy.py`，支持暂停记录、排除应用、只保存文件名不保存路径、清空活动记录。
  - 新增 `habits/sampling.py`，提供非 UI 的定时采样服务 `ActivitySamplingService`。
  - 新增 `confirmation/`，封装前端可用的动作级确认流、拒绝/本次/永久确认和 action trust 写入。
  - `workspace/` 已支持用户反馈 refine，并可保存工作区建议为 recipe。
- 新增/扩展测试覆盖上述能力，当前完整验证：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `183/183` tests OK。
- 已再次检查单文件行数，当前最大 Python 文件仍约 `298` 行，低于 `400` 行限制。
- 开始升级下一步行为预测：避免机械推荐“继续当前项目/app”，改为紧急待办、未完成工作区方案、恢复中断、习惯时间、上下文补全等更有行动价值的策略。
- 完成下一步行为预测升级：`NextActionPredictor` 已按紧急待办 -> 未完成工作区 -> 恢复中断 -> 习惯时间 -> 上下文补全 -> 待办兜底的顺序生成建议，且不再把仅有前台 app 当成“继续使用”的预测。
- 新增 `habits/context.py`、`habits/patterns.py`、`workspace/drafts.py`，并让 `WorkspaceService` 保存待确认工作区草稿，供 `runtime/data/next_action_prediction.json` 恢复未完成流程。
- 优化 `InputRouter`：用户只输入“继续 / 恢复 / 刚才 / 上次”这类模糊词时，会用当前预测补成具体目标，例如“继续：desktop_assistant 的 UI 设计”。
- 修正恢复中断扫描逻辑：按 `captured_at` 选择最近的有意义活动上下文，避免因为活动历史顺序差异而恢复到过旧记录。
- 补充预测相关测试：紧急待办、待确认工作区、恢复中断、习惯时间、上下文补全、仅前台 app 兜底、模糊继续输入补全均已覆盖。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `188/188` tests OK。
- 开始实现新版前端最小闭环，默认启动入口已切换到小型日常助手 Shell，而不是旧的工程调试面板。
- 新增 `desktop_assistant/ui/shell_controller.py`：把首页 UI 需要的待办状态、预测、输入路由、工作区草稿和活动采样封装成薄控制层。
- 新增 `desktop_assistant/ui/shell_window.py`、`shell_pages.py`、`shell_widgets.py`、`shell_styles.py`：实现右上角水雾方框、小球最小化、右键隐身化、拖动/缩放、问候语、待办数量、状态色背景、预测输入框、三入口、待办页、工作环境页和对话规划页。
- 新版 Shell 已接入 `TodoStore`、`NextActionPredictionStore`、`InputRouter`、`WorkspaceService`、`ActivitySamplingService`，并复用现有 `DryRunWorker` / `ExecuteTraceWorker` 进行规划和执行。
- 保留旧 `FloatingAssistantWindow` 作为工程调试型窗口代码；`desktop_assistant/ui/app.py` 的 `main()` 默认实例化新版 `AssistantShellWindow`。
- 新增 `tests/test_ui_shell.py`，覆盖 Shell 控制器的真实待办快照、模糊“继续”预测补全、工作区草稿保存。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant` 通过；Shell 无界面构建检查通过；定向测试 `tests.test_ui_shell tests.test_ui_state tests.test_workspace_input_router` 通过。
- 完成全量回归：`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `191/191` tests OK；当前最大 Python 文件为 `desktop_assistant/ui/shell_window.py`，`365` 行，仍低于 `400` 行上限。
- 新增根目录启动脚本 `run_desktop_assistant.cmd`，用于用户从桌面会话中直接启动新版 UI；当前工具环境可短暂创建 `Desktop Assistant` 窗口，但命令结束后会清理由工具启动的子进程，因此持久运行需要用户手动执行脚本或命令。
- 修复新版 Shell 小球恢复尺寸问题：从小球恢复面板时会先解除固定尺寸，并在几何恢复期间暂停持久化，避免 `78x78` 或最小尺寸覆盖用户原本的面板大小。
- 将最小化小球升级为 `LivingOrb`：使用 Qt 绘制柔软状态球、呼吸环、轻微形变、眨眼和瞳孔追踪；鼠标靠近时会看向鼠标，远离时自动做空闲视线游移。
- 根据用户审美反馈重绘 `LivingOrb`：小球主体改为固定暖橙色玻璃感，状态色改为外圈光环提示；表情从单眼泛光改为更友好的双眼、微笑和鼠标视线追踪，整体减少生硬感。
- 根据用户进一步反馈将 `LivingOrb` 改为纯 2D 橙色小球：去掉嘴巴和拟 3D 渐变，仅保留两个眼睛、眨眼、轻微呼吸和鼠标视线追踪。
- 修复初始化三入口按钮过于紧凑的问题：提高 Shell 最小面板尺寸，调整菜单页间距与标题，并新增最小尺寸下三按钮不重叠的 UI 测试。
- 根据用户反馈继续简化 `LivingOrb`：移除外圈状态光、白色虚框来源、复杂高光和弧线，仅保留单一橙色圆形、两个眼睛、轻微呼吸/眨眼/看鼠标；小球模式下关闭根容器 `QGraphicsDropShadowEffect`，恢复面板时再启用阴影。
- 继续修正小球视觉：眼睛改为更克制的深色豆形双眼，降低视线追踪位移并去掉眼部高光；小球模式新增圆形窗口 mask，并将 orb 模式 stylesheet 收敛为完全透明根容器，避免圆形外侧残留白色圆角方框。
- 进一步处理用户指出的“小球之外标记框”：新版 Shell 顶层窗口加入 `NoDropShadowWindowHint`，关闭 Windows/Qt 原生窗口边框投影来源；小球阴影改由 `LivingOrb` 自己在圆形画布内绘制。
- 严格复查小球 UI 层级后确认：`LivingOrb` 不绘制白线，orb QSS 不含白色边框，offscreen 抓取小球 `78x78` 边缘像素显示 `opaque_edge_pixels=0`、`whiteish_edge_pixels=0`。因此白框来源应为 Windows/DWM 顶层窗口非客户区边框，而不是 Qt 内容层。
- 新增 `desktop_assistant/ui/native_window.py`，在小球模式通过 `DwmSetWindowAttribute` 关闭非客户区渲染、窗口圆角和 DWM 边框色，并显式清理 `WS_BORDER / WS_CAPTION / WS_DLGFRAME / WS_THICKFRAME`，避免透明顶层窗口出现类似选中标记的白色圆角方框。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant` 通过；定向 UI 状态测试 `tests.test_ui_shell tests.test_ui_state` 通过；offscreen 小球边缘像素检查通过；全量回归 `D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `194/194` tests OK。
- 修复原生白框处理带来的 UI 回归：将小球模式和方框模式的透明/无背景属性分离，小球模式启用 `WA_NoSystemBackground`，回到方框模式时关闭该属性并恢复 `shellRoot` 背景绘制，避免其它页面水雾背景变透明。
- 扩大窗口拖动热区：为 Shell 的非交互区域安装统一拖动 event filter，页面空白、标题和普通文字区域都可拖动；按钮、输入框、列表、文本框、下拉框和 `QSizeGrip` 不触发拖动，以免影响正常操作。
- 根据用户选择的 E 款小球方案重绘 `LivingOrb`：改为橙色绒毛小球，使用短绒毛边缘、柔和毛面纹理、小圆眼和眼部高光；继续保持无嘴巴、无外框、轻微呼吸、眨眼和鼠标视线追踪。
- 新增真实代码渲染预览图 `runtime/ui_previews/orb_furry_e_preview.png`，用于对照生成图和实际 QPainter 落地效果。
- 复查后发现预览和程序小球不一致的主要原因是预览用 `180x180` 渲染，而实际 UI 使用 `78x78` 渲染，原先绘制参数按像素固定，导致真实小球比例、绒毛和眼睛都变样。
- 已将 `LivingOrb` 改为固定 `180x180` 设计稿内部渲染，再平滑缩放到实际窗口尺寸；同时延后启动眨眼，避免程序刚出现时抓到半眯眼状态。
- 新增真实应用尺寸对照图 `runtime/ui_previews/orb_actual_78_after.png`，左侧为程序 `78x78` 小球放大，中间为同一代码 `180x180` 预览，右侧为真实大小。
- 修复“小球切回方框后，圆角边框后出现白色矩形背景”的回归：复现后确认直接打开方框时角落透明，但经过 `orb -> panel` 后角落变成不透明浅灰/白色，根因是方框模式恢复时顶层透明区域没有正确恢复。
- 方框模式窗口属性改为：顶层 `WA_NoSystemBackground=True`，根容器 `WA_TranslucentBackground=True`，根容器 `WA_NoSystemBackground=False`。该组合能让圆角外侧保持透明，同时保留 `shellRoot` 水雾背景绘制。
- 新增 UI 回归断言：`orb -> panel` 后窗口角落 alpha 必须小于 20，中心主体 alpha 必须大于 180；新增真实预览 `runtime/ui_previews/panel_after_orb_fixed.png`。
- 将 Shell UI 固定文案集中到 `desktop_assistant/ui/shell_text.py`，首页、菜单、待办页、工作区页、对话页文案后续统一从该文件维护。
- 新增 `desktop_assistant/ui/shell_todo.py`，把待办页交互从 `shell_window.py` 抽离，保持 `shell_window.py` 为窗口状态和执行流基座；当前最大 UI 文件为 `shell_window.py`，约 `353` 行。
- 待办页产品化：新增提醒时间输入、优先级选择、重要标记、完成、删除、推迟 30 分钟、点击待办生成工作区建议预览。
- 新增 `desktop_assistant/todo/time_parser.py`，支持 `30m`、`1小时`、`18:30`、`明天 09:00` 和 ISO 时间输入。
- 修正中文输入路由、下一步行为预测、工作区建议和问候语中的中文常量，避免中文命令或预测文案因乱码常量失效。
- 扩展 UI/后端回归：Shell 文案无乱码、待办页最小尺寸布局、带时间和优先级添加待办、推迟待办、点击待办生成工作区建议、待办标题推断 URL 工作区资源。
- 继续补齐“待办 -> 工作区建议 -> 确认 -> 执行 -> 结果回写”闭环：待办详情区现在展示动作级确认摘要，并提供 `拒绝`、`本次执行`、`永久确认` 三个入口。
- 新增 `WorkspaceExecuteWorker`，后台执行已确认的工作区 `ActionPlan`，避免 UI 阻塞；执行结果会形成 `WorkspaceExecutionSummary`。
- 执行完成后会把 `trace_id`、`status`、`message` 写回待办 `last_execution`；拒绝执行也会写入拒绝记录，便于后续追踪。
- 永久确认仅在 confirmation flow 允许 `TRUST_ALWAYS` 时启用，并写入 action-level trust；低风险动作默认只启用本次执行。
- 新增回归测试：工作区执行 worker 成功路径、待办执行结果回写、待办详情确认摘要和按钮启用状态。
- 继续产品化工作区建议编辑体验：待办页新增可勾选动作列表，用户可以取消某个建议动作，也可以新增应用/网页/文件/文件夹/项目动作。
- 新增 `desktop_assistant/ui/shell_workspace_actions.py`，负责 `WorkspaceSuggestion` 与可编辑动作列表之间的转换，并根据勾选状态生成最终 `ActionPlan`。
- `本次执行` / `永久确认` 现在执行编辑后的最终计划；`TodoExecutionRecord` 新增 `executed_actions`，保存最终执行动作列表。
- 扩展回归测试：取消建议动作、手动新增动作、最终计划过滤、执行记录保存最终动作。

## 2026-05-01

- 完成待办详情编辑闭环：点击待办后可编辑标题、说明、提醒时间、优先级和重要标记，保存后刷新首页统计、最近任务和状态色。
- 新增 `取消待办`：与删除分开，只把任务标记为 `cancelled` 并从开放列表隐藏，保留历史记录用于追溯。
- 扩展 Shell 控制层：`AssistantShellController` 新增 `update_todo()`、`cancel_todo()`，编辑时保留待办已有 workspace、确认状态和上次执行记录。
- 补充 UI/后端回归测试：覆盖待办详情编辑、取消待办、首页红色紧急状态刷新、最小尺寸布局、编辑不破坏 workspace 和 `last_execution`。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant tests` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `209/209` tests OK；最大 Python 文件为 `desktop_assistant/ui/shell_window.py`，`400` 行。
- 完成待办与工作区绑定产品化：待办详情新增 `电脑完成` 开关和 `保存绑定`，用户可把当前勾选后的应用/网页/文件/文件夹/项目动作保存回待办。
- 新增 `desktop_assistant/todo/workspace_binding.py`，集中处理 `ActionPlan -> TodoWorkspaceHint` 的转换和去重，避免 UI 层散落字段映射。
- `TodoStore.update_workspace()` 保存绑定时会重置 workspace 确认状态和 trusted action keys，防止改过动作后沿用旧确认；同时保留 `last_execution` 方便追溯。
- 扩展回归测试：覆盖 controller 保存绑定、store 保存绑定与确认重置、UI 从编辑后的动作列表保存绑定，以及保存后工作区预览优先使用绑定。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant tests` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `212/212` tests OK；最大 Python 文件仍为 `desktop_assistant/ui/shell_window.py`，`400` 行。
- 完成待办工作区执行前确认页：点击 `准备工作区` 后进入 `执行前确认`，按应用/网页/文件/文件夹/项目分组展示最终动作，并显示风险、确认状态和原因。
- 新增 `desktop_assistant/ui/shell_workspace_confirm.py`，集中承载确认页预览、拒绝、本次执行、永久确认和执行结果展示，避免继续增大 `shell_todo.py`。
- 扩展 Shell UI 回归测试：覆盖从待办进入确认页、分组预览内容、拒绝/本次执行/永久确认按钮状态。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant tests` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `213/213` tests OK；最大 Python 文件仍为 `desktop_assistant/ui/shell_window.py`，`400` 行。
- 根据产品体验反馈重构待办入口：`待办任务清单` 现在默认只显示待办列表和轻量快速新增，不再直接暴露编辑、工作区绑定和执行确认等复杂控件。
- 新增独立待办详情页：点击某个待办后才进入详情操作页，承载编辑、完成/推迟/取消、工作区动作编辑、保存绑定和准备工作区。
- 新增 `desktop_assistant/ui/shell_todo_items.py`，为待办列表项按紧急程度着色：紧急/30 分钟内偏红，高优先级/3 小时内偏橙，普通偏黄，低优先级偏绿。
- 扩展 UI 回归测试：覆盖列表页保持简洁、详情页点击后进入、颜色区分紧急程度，以及既有详情编辑/工作区绑定流程在新分层下继续可用。
- 修复待办详情页在小窗尺寸下控件被压缩、挤在一起的问题：进入详情页和执行前确认页时自动扩展到工作尺寸。
- 将待办详情页内容放入滚动容器，用户即使手动缩小窗口，也通过滚动查看复杂控件，而不是让输入框、列表和按钮互相重叠。
- 扩展 Shell UI 回归测试：从 `430x360` 的待办列表页点击待办后，断言窗口扩展到工作尺寸且详情滚动容器存在。
- 优化待办详情页的工作区动作目标输入：应用目标从普通文本框升级为可编辑下拉框，选项来自 `app_inventory.json`，并在添加前把用户输入规整为应用清单里的正式名称。
- 文件和文件夹动作新增系统选择入口：`选择文件` / `选择文件夹` 会打开资源管理器选择器并回填完整路径，用户仍可手动粘贴路径。
- 新增 `desktop_assistant/ui/shell_workspace_target.py`，集中维护应用补全、文件/文件夹选择和动作目标占位文案切换。
- 扩展 Shell UI 回归测试：覆盖应用下拉选项、`qq -> QQ` 这类 inventory 解析，以及文件/文件夹选择器回填路径。
- 根据界面观感反馈取消方框面板半透明效果：`shellRoot` 主背景改为完全不透明，方框模式强制 `windowOpacity=1.0`，只保留圆角外侧透明和小球独立隐身透明逻辑。
- 更新 UI 回归断言：`orb -> panel` 后窗口中心主体 alpha 必须接近完全不透明，避免旧 opacity 配置让面板继续发虚。
- 新增 `desktop_assistant/ui/localization.py` 作为展示层本地化映射，内部协议字段仍保持英文，UI 输出时再把动作、风险、状态、确认等关键词转换为中文。
- `对话与规划` 的 `summary_to_plain_text()` 已中文化核心标签：追踪、状态、意图、计划来源、策略检查、审查、计划动作、失败代码和建议处理等。
- Worker 进度提示从英文改为中文，避免真实/模拟后端运行过程中短暂出现英文状态。
- 扩展 UI view model 回归测试：确认规划摘要出现中文关键词，并且不再裸露 `Policy risk:`、`open_app -> ...` 这类英文展示文本。
- 自然化 `工作区建议` 产品体验：新增 `desktop_assistant/ui/shell_workspace_view.py`，将 `WorkspaceSuggestion` 渲染为“目标、准备内容、用途、风险”的中文建议，而不是 `open_url -> target` 这类调试输出。
- 新增 `desktop_assistant/ui/shell_workspace_flow.py`，把生成建议、按意见调整、保存方案和确认执行从 `shell_window.py` 拆出；`shell_window.py` 当前回落到 400 行以内。
- 通用工作区页的 `确认执行` 已接入现有动作级确认页，非待办来源也能确认并返回工作区页；待办详情里的工作区摘要也改为中文动作名。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant tests` 通过；`D:\anaconda3\envs\app\python.exe -m unittest tests.test_ui_shell -v` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `218/218` tests OK。
- 继续推进工作区建议产品化：工作区页新增可勾选动作列表，支持新增动作、更新选中动作、删除选中动作；应用目标复用 inventory 补全，文件/文件夹目标复用系统选择器。
- `保存方案`、`按意见调整` 和 `确认执行` 现在都会先读取编辑后的动作列表，避免用户明明取消或修改了动作，最终仍按原始方案执行。
- `shell_workspace_actions.py` 扩展为工作区页和待办详情页共享的动作编辑转换层，勾选后的 `ActionPlan` 会同步回 `WorkspaceSuggestion.resources`，保证后续 refine 不把用户删除的动作悄悄带回来。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant tests` 通过；`D:\anaconda3\envs\app\python.exe -m unittest tests.test_ui_shell -v` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `219/219` tests OK。
- 将 `保存方案` 升级为可复用工作区方案库：当前编辑后的工作区动作会保存为 `scenario="workspace"` 的 `WorkflowRecipe`，而不只是临时草稿。
- 工作区页新增 `选择已有工作区方案` 下拉和 `加载方案` 按钮；加载后会把 recipe 转回 `WorkspaceSuggestion`，重新填入目标、自然语言说明和可编辑动作列表。
- `WorkspaceService` 新增 workspace recipe 列表与 recipe -> suggestion 转换方法；UI 只通过 controller 调用，不直接读取 recipe JSON。

### 2026-05-01

- 推进“执行反馈产品化”：新增 `desktop_assistant/ui/execution_feedback.py`，将工作区执行结果从内部日志改为自然中文反馈。
- 工作区成功结果现在显示为“执行完成 / 已打开网页 / 已打开应用”等用户语言；混合成功和失败会显示“部分完成”，而不是笼统失败。
- 常见失败原因新增产品化解释：应用不在清单、可执行文件缺失、进程已运行但无窗口、窗口未找到、文件/文件夹/项目不存在、浏览器拒绝 URL、handler 未接入等。
- 失败反馈继续保留 `原因代码`，方便后续排查和测试，但 UI 主要呈现用户可理解的原因与建议。
- 待办详情页和工作区确认页现在使用中文执行状态，并对 worker 异常做可读化压缩，避免直接把完整 traceback 推到用户面前。
- 新增 `tests/test_ui_execution_feedback.py`，覆盖成功、部分完成、失败诊断三类核心反馈。

### 2026-05-01

- 新增 `docs/handoff-2026-05-01.md` 作为上下文交接文档，方便后续会话在上下文达到极限后继续接力。
- 交接文档记录了运行环境、启动命令、验证命令、当前能力、关键文件地图、已知风险、建议下一步和接力提示。
- 文档明确不记录 API Key 等敏感信息，并保留“单个 `desktop_assistant/` Python 文件一般不超过 400 行”的维护约束。
- 应用户要求补全 real provider 交接细节：记录 `runtime/data/model_provider.json` 路径、配置摘要、加载优先级、验证命令和 real 模式入口；仍不把完整 API Key 写入文档。
- 进一步明确下个会话做 real 测试时不需要知道明文 key：直接使用 `ProviderConfigStore`、`build_orchestrator(ai_backend="real")` 或现有 real 命令读取本地配置；只有配置文件缺失时才需要用户重新提供。

### 2026-05-01

- 开始缩短核心使用路径：首页输入明确打开/启动/聚焦类请求时，先生成工作区建议；如果有可执行动作，直接进入 `执行前确认` 页面。
- `InputRouter` 新增直接工作区动作识别，覆盖 `打开`、`启动`、`运行`、`聚焦`、`切到`、`进入`、`准备` 以及基础英文 open/launch/start/focus。
- `ShellWorkspaceFlowMixin._submit_text()` 不再对工作区类输入先停在编辑页，而是走 `_quick_prepare_workspace()`：有动作直接确认，没有动作且属于直接打开类请求时降级到原对话/规划链路。
- 灰色预测若指向未完成工作区草稿，Shell 现在会按 `target_id` 从 `WorkspaceDraftStore` 加载原草稿并直接进入确认页，避免重新猜测和丢失用户已编辑方案。
- 灰色预测若指向具体待办，且该待办已有或可推断出工作区动作，Shell 会直接进入该待办的执行前确认页；没有动作时才回到待办详情。
- 新增 `WorkspaceDraftStore.get_pending()`、`WorkspaceService.pending_draft()`、`AssistantShellController.pending_workspace_draft()`，作为预测恢复草稿的短路径接口。
- 扩展回归测试：覆盖首页打开 URL 直达确认、接受未完成工作区预测直达确认、接受待办预测直达确认，以及直接打开类输入路由。
- 新增 `tests/test_ui_workspace_plan.py` 用例覆盖保存方案、下拉选中、加载方案、以及加载后进入执行前确认。
- 优化待办到工作区的核心使用路径：点击待办后，详情顶部先展示“这个任务建议先这样准备”的自然语言建议，用户可以直接点 `现在准备` 进入执行前确认。
- 将原 `准备工作区` 按钮改为 `现在准备`，并在详情文案中提示“不合适就取消勾选或补充动作”，让配置型动作列表变成微调入口，而不是第一感知。
- 新增 `desktop_assistant/ui/shell_todo_view.py`，集中渲染待办详情、执行记录和确认摘要；`shell_todo.py` 行数回落，继续只负责交互流程。

### 2026-05-01

- 继续补齐执行失败后的产品化补救闭环：新增 `desktop_assistant/ui/execution_remedies.py`，根据失败原因生成“刷新应用清单 / 5 秒后再聚焦 / 刷新窗口列表 / 打开搜索页 / 重新选择路径 / 查看能力状态”等补救项。
- 工作区执行前确认页新增补救按钮区，失败后最多展示 3 个可操作按钮；可直接触发低风险补救动作，并继续复用 `workspace_execution_feedback()` 展示补救结果。
- `workspace_execution_feedback()` 现在会把可执行补救建议写入反馈文本，避免只有按钮没有解释，也方便测试和日志追踪。
- 增强 `answer_query` 结果结构：metadata 新增 `attempted_sources`、`attempted_source_count`、`confidence_reason`、`fallback_reason`、`checked_at`，并在 UI 反馈中展示查询结果、可信度、来源和兜底搜索。
- 窗口/应用状态展示改为产品语言：窗口详情改为中文字段，窗口列表成功后展示“可见窗口数量”和“当前前台”，失败仍走统一执行反馈。
- 能力目录增强健康视图：`CapabilitySummary` 新增健康状态、风险说明、调试建议；能力列表从英文协议格式改成中文产品格式。
- 配方系统展示继续产品化：方案列表、详情、动作表和检查结果改为中文表达，并保留底层 action/risk 数据用于保存和执行。
- 待办详情页顶部摘要和工作区动作列表高度略增，减少详情页小窗下控件挤压感。
- 真实 Planner 稳定性修正：`RealPlanner` 归一化 `list_windows` 的 target 为 `visible`，避免真实模型输出“当前桌面可见窗口 / 当前可见桌面窗口”等同义文本导致评测不稳定。
- 质量评测集新增窗口/工作区用例：`list_visible_windows`、`minimize_cursor_window`、`writing_workspace_setup`。
- fake 质量评测完成：12 个用例、2 轮，共 24 次 dry-run，全部通过且稳定；报告 `runtime/reports/quality_fake_2026-05-01.json`。
- real 多轮质量评测完成：5 个用例、2 轮，共 10 次 dry-run，质量检查 0 失败、runtime 0 失败、fallback 0；发现并修复 `list_windows` target 稳定性问题。
- real targeted 复测完成：`list_visible_windows` 2 轮稳定通过，strict schema 通过；报告 `runtime/reports/real_quality_list_windows_after_normalize_2026-05-01.json`。
- 新增/更新回归测试：执行补救、查询反馈、能力健康详情、配方产品文案、窗口中文反馈、real planner list_windows 归一化、工作区失败补救按钮。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant tests` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `234/234` tests OK；最大 Python 文件为 `desktop_assistant/ui/shell_pages.py`，`376` 行，仍低于 `400` 行上限。

### 2026-05-01

- 按“除 UI 审美统一外全部推进”的要求补齐核心地基项：核心使用路径、补救按钮闭环、信息查询质量、待办工作区融合、配方短路径、窗口状态确认和代码维护。
- 拆分 `desktop_assistant/ui/shell_pages.py`：新增 `shell_page_helpers.py`、`shell_todo_pages.py`、`shell_workspace_pages.py`，`shell_pages.py` 从 376 行降到约 102 行，继续满足单文件不超过 400 行的维护约束。
- 补救按钮从“提示文案”推进到可闭环操作：路径失败会回到对应编辑页并预填失败目标；能力未接入会在确认页展开对应能力状态；刷新清单、延迟聚焦、刷新窗口、打开兜底搜索继续走执行器和统一反馈。
- 信息查询增强：`answer_query` 增加验证状态、交叉来源、来源概况；多来源网页摘要会标记为中可信，UI 展示“验证状态 / 来源概况 / 来源 / 兜底搜索”。
- 待办与工作区进一步融合：待办推断资源时同时读取标题、说明、提醒/截止时间、优先级和重要标记；待办详情新增一句自然工作区建议，降低用户理解成本。
- 配方系统短路径增强：首页输入若命中已保存的 workspace recipe，会直接加载方案并进入执行前确认页，不再要求用户先进入工作区页手动选择。
- 窗口动作执行后新增后状态确认：metadata 写入 `verification_status`、`verified_window`、`foreground_window`；执行反馈展示窗口是否已最小化/最大化/恢复、当前前台等信息。
- 新增/更新回归测试：路径补救导航、多来源查询、查询验证展示、窗口动作状态确认、待办说明参与工作区推断、首页命中已保存方案直达确认。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant tests` 通过；`D:\anaconda3\envs\app\python.exe -m unittest tests.test_web_query tests.test_ui_execution_feedback tests.test_windows_executor -v` 通过；`D:\anaconda3\envs\app\python.exe -m unittest tests.test_workspace_input_router tests.test_ui_workspace_plan -v` 通过；`D:\anaconda3\envs\app\python.exe -m unittest tests.test_ui_shell -v` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `240/240` tests OK。

### 2026-05-01

- 根据新的视觉方向，将小球从“橙色绒毛双眼”切换为“透明背景律动胶囊音波入口”，降低吉祥物感，更贴近桌面管理助手的状态入口定位。
- 当前运行实现位于 `desktop_assistant/ui/shell_orb.py`：不再绘制橙色球壳、底板或阴影背景，只保留中间多根胶囊柱状条随时间律动，保留隐身模式。
- 音波颜色直接使用首页/方框背景的状态色，即待办紧急度驱动的红、橙、黄、绿逻辑，避免继续固定橙色印象。
- 继续微调音波入口：胶囊柱从 11 根减少到 7 根，外侧新增同状态色的轻圆形边框用于界定范围，仍不填充背景。
- 原小球实现已备份为 `desktop_assistant/ui/shell_orb_furry_backup.py`，后续可回退或对比。
- 更新 UI 回归测试：覆盖透明角落、外侧圆形边框、中心状态色音波、隐藏模式 alpha 降低、胶囊音波高度边界和律动变化；保留小球/方框切换测试。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant tests` 通过；`D:\anaconda3\envs\app\python.exe -m unittest tests.test_ui_shell -v` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `240/240` tests OK。
- Shell 新增系统托盘与桌面小球共存能力：增加 `desktop_assistant/ui/shell_tray.py`，运行时若系统托盘可用，会常驻一个托盘图标和右键菜单，同时继续保留桌面悬浮小球；托盘支持“显示面板 / 显示小球 / 退出”，单击托盘图标可直接恢复面板。
- `desktop_assistant/ui/shell_window.py` 已接入托盘初始化、状态色驱动托盘图标、基于待办快照的托盘提示文本，以及从托盘恢复面板/切回小球的入口；关闭窗口时会同步清理托盘图标，避免遗留无效入口。
- 补充 UI 回归测试：`tests/test_ui_shell.py` 新增托盘存在性与“从托盘把小球恢复为面板”的断言；并重新验证原有小球位置恢复与 `ui_state` 持久化测试均保持通过。
- 修正透明音波小球圆形边框偏白问题：边框主描边不再与白色混合，改为直接使用待办紧急度状态色；补充回归断言，确保圆环像素跟随状态色而不是白色倾向。验证结果：`compileall` 通过，`tests.test_ui_shell` 31/31 OK。
- 根据用户澄清调整音波小球视觉规则：圆形边框应固定为白色，用于界定透明小球范围；中间 5 根胶囊音波仍跟随待办紧急度状态色。同步更新回归断言、spec 与交接文档。
- 继续优化音波小球边界可见性：圆形边框从单层白线升级为双层反差边框，外层低透明度暗色托底、内层白色细线；不采样桌面背景，避免隐私/性能/闪烁风险，音波仍只表达待办状态色。

### 2026-05-02

- 推进 Shell 展开页视觉统一：新增统一 `shellSurface` 容器，将待办列表、快速新增、待办详情、工作区建议、执行确认、动作编辑和按钮区收敛到同一套低透明白底、细边框、8px 圆角的视觉语言。
- 优化待办详情页和工作区页的动作编辑区：将“动作类型/目标输入/选择文件”与“新增/更新/删除/保存”等按钮拆成上下两行，减少小窗口下的拥挤和遮挡。
- 工作区页进入时改用工作面板尺寸策略，和待办详情/确认页保持一致，避免新增 surface 后输入框或下拉框被压缩。
- 新增 UI 回归测试：覆盖 Shell 工作页 surface 数量、工作区编辑控件可用高度、目标输入行与按钮行不重叠。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant tests` 通过；`D:\anaconda3\envs\app\python.exe -m unittest tests.test_ui_shell tests.test_ui_workspace_plan -v` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `241/241` tests OK。当前最大 Python 文件为 `desktop_assistant/ui/shell_todo.py`，`368` 行，仍低于 400 行上限。
- 继续按用户反馈微调透明音波小球：外侧圆形边框进一步外移并增强边界感，胶囊音波柱从 7 根减少到 5 根，减少密集感；仍保持透明背景，并继续使用首页/方框状态色驱动音波和边框颜色。
- 验证结果：`D:\anaconda3\envs\app\python.exe -m compileall -q desktop_assistant tests` 通过；`D:\anaconda3\envs\app\python.exe -m unittest tests.test_ui_shell -v` 通过；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过，当前 `240/240` tests OK。
# 2026-05-02 Codex 接力补强

- 修复工作区草稿 refine 身份连续性：`WorkspaceSuggestion` refine 后保留原 `id` / `created_at`，累积 `user_feedback`，待确认草稿与预测不再断链。
- 修复待办执行记录失真：`executed_actions` 改为记录真实执行结果，包含 `status` / `message`，不再把失败动作写成已执行。
- 修复 `TRUST_ALWAYS` 延迟生效：确认服务在同进程内即时刷新 trusted keys。
- trace 持久化 `ai_backend` 与 `provider_config_path`，执行已保存 trace 时继承原 backend，不再退回 fake。
- `prepare()` 阶段异常结构化：`context / planner / policy / reviewer` 失败会生成 failed trace，写入 `prepare_error`，debug snapshot 和 UI 摘要可读。
- 本地 JSON 存储统一改成原子写：新增 `storage/json_files.py`，核心 JSON store 通过临时文件 + `os.replace()` 写入。
- 面向用户态的 JSON store（todo / drafts / recipes / prediction / projects / activity / privacy / ui_state）支持坏文件自动隔离 `*.corrupt` 并回退默认值。
- 关键配置与目录 JSON（provider config / action trust / capability catalog / app inventory）支持坏文件隔离并生成更清晰的错误。
- 新增恢复事件日志 `storage/recovery_events.py`：记录恢复来源、类别、原文件、隔离文件与原因。
- Shell 首页增加最近一次恢复提示；桌面调试窗口增加 `Recovery` 列表块，可查看最近多条恢复记录。
- `capability_debug` 诊断报告增加恢复事件统计与最近恢复条目。
- UI worker 失败信号升级为结构化 `WorkerFailure`，Shell / 待办 / 工作区确认页 / 窗口调试窗口统一消费。
- 工作区“保存方案”后仍保持 pending draft，不再把当前 suggestion 从待确认队列移除。
- `TodoStore / WorkspaceDraftStore / RecipeStore` 默认路径已切换到 `runtime/data/desktop_assistant.db`，并保留传入 `.json` 路径时的兼容模式。
- `storage/sqlite.py` 新增 versioned migration 入口与 `todo_items / workspace_drafts / workflow_recipes` 表；数据库连接启用 `WAL`。
- `TodoStore` 的 SQLite 模式已改为按条 `SELECT / UPSERT / DELETE`，不再依赖整表覆盖；`WorkspaceDraftStore` 与 `RecipeStore` 也支持 SQLite 模式。
- 默认 SQLite 模式下，若旧版 `todos.json / workspace_drafts.json / recipes.json` 存在且新表为空，会首次访问时自动导入旧 JSON 内容。
- SQLite migration 机制继续补强：migration 现已具备 `version / name / description` metadata，新增 `schema_migrations` 历史表，并支持从旧 `user_version` 自动回填迁移历史。
- 兼容入口模块（`capabilities.py / capability_store.py / capability_executor.py / recipes.py / project_locator.py / prompt_templates.py`）本轮继续保留，并补了兼容导出测试；目录整编采用“规范包入口 + 兼容层”策略，而不是一次性大重命名。

- 新增/更新测试覆盖：
  - JSON 原子写与失败回滚
  - 损坏 JSON 隔离与恢复事件记录
  - provider config / action trust / capability catalog / app inventory 坏文件恢复
  - prepare 阶段结构化失败 trace
  - 恢复事件视图模型与桌面 Recovery 面板
  - worker 结构化异常文本
  - 工作区 pending draft 连续性与执行记录真实结果
  - Todo / Draft / Recipe 的 SQLite 默认路径与兼容行为
  - SQLite migration 历史记录与旧库回填
  - 兼容导出层与规范包入口一致性

- 当前回归：
  - `python -m unittest discover -s tests -v`
  - 最新结果：中途修复过 1 个 `test_ui_workspace_plan` 失败（保存 recipe 后 pending draft 状态丢失），以及 2 个 Windows/WAL 相关的 SQLite 测试清理问题，均已定向修复；本轮最终再次执行全量回归确认。

- ????????????????????????Shell ???????????????????? 1/5??? 4/5????????????????????????

### 2026-05-04 暮蝶 Q 版桌宠孵化落地

- 使用 hatch-pet 完成暮蝶 Q 版桌宠修复孵化：9 行动作均已重新生成/记录，并通过最终 QA。
- 因暮蝶蓝粉/粉紫发色与原品红 chroma key 冲突，修复阶段将 run 的 chroma key 从 #FF00FF 调整为 #00FF00，减少头发边缘被误判为背景相近色。
- 最终产物已打包到 runtime/pets/mudie，包含 pet.json 与 spritesheet.webp；Shell 最小化入口会优先加载该运行时宠物包。
- QA 产物位于 output/hatch-pet/mudie/qa/contact-sheet.png，最终 spritesheet 位于 output/hatch-pet/mudie/final/spritesheet.webp。
- 同步清理 shell_orb.py 的宠物包发现逻辑，保留 DESKTOP_ASSISTANT_PET_DIR 覆盖能力、runtime/pets/mudie 默认路径和用户 .codex/pets/mudie 兜底路径。
- 验证结果：hatch-pet finalize_pet_run.py --skip-videos 通过；桌宠加载、小球恢复、托盘恢复 3 条 UI 回归测试通过。

### 2026-05-04 暮蝶主题 Shell 视觉统一

- Shell 展开态从整块待办状态色背景调整为暮蝶系雾面渐变面板：墨紫灰底、雾蓝边、花瓣粉交互高光，状态色只参与边框/hover/选中态，不再整屏染色。
- 待办页、待办详情、工作区页、工作区确认页、聊天页共享同一套 shellSurface、输入框、列表、按钮、滚动条和标题样式；页面标题旁新增克制的蓝粉蝶翼短标。
- 桌面调试/health 主窗口同步调整到同一色彩体系，但保持工具页克制密度，避免影响排查效率。
- 新增样式回归测试，确认 Shell 主题包含暮蝶主题色、渐变面板、surface 卡片与状态色混入规则。
- 验证结果：compileall 通过；Shell 主题、菜单、待办页、工作区页、桌宠加载 6 条关键 UI 回归测试通过。

## 2026-05-04 退出语义修正

- 桌宠常驻后，页面右上角关闭按钮不再只是关闭面板，而是调用全局退出流程。
- 最小化按钮继续保持“收起到桌宠”的语义，桌宠仍然常驻。
- 桌宠右键菜单新增“打开面板 / 半透明模式 / 退出应用”，用户不需要通过任务管理器结束进程。
- 托盘菜单的退出也统一走全局退出流程，会同时关闭面板、桌宠并退出 QApplication。
- 已补充 UI 测试覆盖持久桌宠退出和桌宠退出回调。

## 2026-05-04 桌宠退出交互再调整

- 页面右上角关闭按钮按用户新预期调整为“收起面板到桌宠”，不会关闭桌宠，也不会退出应用。
- 真正退出应用只保留在桌宠右键入口，避免页面关闭和应用退出语义混在一起。
- 桌宠右键不再使用系统黑色菜单，改为无边框聊天气泡式菜单。
- 气泡选项包括“打开面板 / 变成半透明或恢复清晰 / 退出应用”，每个选项都是清晰可点击的圆角气泡按钮。
- 托盘菜单移除退出入口，仅保留显示面板和显示桌宠。
- 已补充测试覆盖页面关闭保留桌宠、气泡菜单文字可读、全局退出仍可关闭桌宠。

## 2026-05-04 桌宠气泡菜单体验优化

- 右键桌宠打开气泡菜单后，如果不想选择任何选项，可以左键点击桌宠直接收起气泡。
- 气泡菜单从“一个大框包住多个按钮”调整为“标题气泡 + 每个选项独立气泡”。
- 气泡视觉改为与应用和暮蝶桌宠统一的蓝紫渐变、粉色高光与暖黄色退出气泡。
- 气泡关闭时会清理窗口引用，避免隐藏气泡残留。
- 已补充测试覆盖气泡可读性、独立气泡样式和点击桌宠关闭气泡。

## 2026-05-04 桌宠气泡独立性修正

- 进一步修正右键菜单视觉，不再用同一个大容器模拟气泡列表。
- 新增独立 `PetBubbleButton`，每个选项自己绘制圆角气泡和朝向桌宠的小尾巴。
- 标题也改为独立 `PetBubbleLabel`，透明菜单窗口只负责摆放，不参与视觉边框。
- 选项气泡不再被拉满为同宽列表，退出气泡宽度独立，视觉上更像从桌宠旁边冒出的多条对话。
- 已补充测试确认选项控件类型、标题气泡类型和选项宽度小于透明容器宽度。

## 2026-05-04 桌宠气泡孵化动画

- 按用户描述把右键菜单升级为“从桌宠头部冒泡”的动画，而不是静态气泡列表。
- 透明菜单窗口记录桌宠头部锚点，每个气泡从该锚点方向冒出两颗小球，再长成完整气泡。
- 标题气泡和每个选项气泡都有独立 reveal progress，并按短暂 stagger 依次出现。
- 气泡控件保留透明承载区域用于绘制头部到气泡的冒泡路径，真正视觉宽度由 `visual_width()` 表达。
- 已补充测试覆盖气泡 reveal 状态、source point 和独立气泡结构。

## 2026-05-04 桌宠气泡去除小三角

- 按用户反馈移除每个选项气泡边缘突出的三角尾巴。
- 气泡本体改为干净圆角胶囊，仍保留从桌宠头部冒出的小球动画来表达来源。
- 同步调整文字区域和冒泡终点，避免保留原三角预留空间。
- 已确认不再存在 `QPainterPath`/`drawPath` 三角绘制逻辑，相关测试通过。

## 2026-05-04 桌宠气泡外层边框隐藏

- 按用户反馈隐藏包住所有气泡的外层大边框，外层菜单窗口只作为透明承载层。
- 为 `PetBubbleMenu` 增加 `NoDropShadowWindowHint`、`WA_NoSystemBackground` 和显式透明无边框样式。
- 保留每个独立气泡自己的圆角边框和从桌宠头部冒出的动画。
- 已补充测试锁定外层透明属性，并通过 offscreen 像素检查确认窗口四角 alpha 为 0。

## 2026-05-04 桌宠气泡外框根因修复

- 用户实测仍能看到外层边框，判断根因是独立顶层 `Qt.Popup`/工具窗口在 Windows 上可能存在原生边界。
- 将气泡菜单从独立顶层窗口改为 `DesktopPetWindow` 内部子控件，彻底避免系统绘制额外窗口边框。
- 右键时桌宠窗口临时扩展以容纳气泡，桌宠自身视觉位置保持不变；关闭气泡后窗口缩回原始桌宠大小。
- 已补充测试确认 `bubble_menu.parentWidget() is pet`、`bubble_menu.isWindow() == False`，并确认关闭后窗口尺寸恢复。

## 2026-05-04 桌宠气泡命中范围修复

- 用户反馈气泡打开后，鼠标放到桌宠身上会高亮“打开面板”，且左键点桌宠无法关闭气泡。
- 根因是按钮控件为了承载冒泡路径占满透明菜单宽度，透明区域也参与鼠标命中。
- 将 `PetBubbleButton`/`PetBubbleLabel` 控件真实尺寸收缩到气泡视觉宽度，冒泡路径由父级透明层绘制。
- 透明菜单非按钮区域左键点击会转交给桌宠关闭气泡，不再误触发按钮。
- 已补充测试确认按钮右边界不覆盖桌宠区域，运行检查显示按钮右边界 263、桌宠起点 264。

## 2026-05-04 桌宠气泡拖动体验修复

- 用户反馈气泡打开后想直接拖动桌宠，必须先点一下关闭气泡，交互不顺。
- 左键按下桌宠时如果气泡存在，现在会先关闭气泡，然后同一次鼠标按下立即进入拖动状态。
- 后续 MouseMove 会继续沿用这次按下的拖动偏移，不需要第二次点击。
- 已补充测试覆盖“关闭气泡同时开始拖动”，运行模拟确认位置可在同一次拖动中移动。

## 2026-05-04 桌宠气泡拖动接管修复

- 用户实测仍然只关闭气泡、不拖动桌宠，判断后续 MouseMove 可能没有继续进到桌宠子控件的 eventFilter。
- 气泡关闭后的左键按下现在由桌宠窗口接管，`eventFilter` 返回 True，避免事件链被窗口收缩打断。
- 新增 `DesktopPetWindow.mouseMoveEvent` / `mouseReleaseEvent` 兜底接续拖动。
- 已补充测试覆盖 press 由 eventFilter 接管、move 走窗口级 mouseMoveEvent 的真实场景。

## 2026-05-04 桌宠气泡展开残影修复

- 用户反馈打开气泡时另一端会闪过桌宠残影。
- 判断根因是桌宠窗口扩展/移动/子控件重定位的瞬间，旧绘制区域可能被系统短暂保留。
- 展开气泡时现在会临时冻结窗口更新并隐藏桌宠子控件，完成窗口尺寸、位置和 pet 子控件几何同步后再显示并强制 repaint。
- 收起气泡时同样使用冻结更新流程，避免缩回时残留旧图像。
- 修复 `_expand_for_bubble_menu` 中读取未同步 `self.width()` 导致窗口没有正确展开的问题，改为显式 target width/height。
- 已补充测试确认展开后 pet 子控件位置与 `_pet_offset` 一致，收起后恢复到 `(0, 0)`。
## 2026-05-04 桌宠气泡残影与拖动修复

- 用户反馈：气泡出现时仍有桌宠残影，并且气泡状态下直接拖动桌宠只会关闭气泡，桌宠不会跟随移动。
- 根因判断：气泡打开时父窗口临时变宽，桌宠本体从窗口左侧移到右侧；透明窗口没有主动清空旧绘制内容时，Windows/Qt 可能保留旧位置一帧，形成残影。另一个问题是关闭气泡后第一次按下事件被处理掉，后续鼠标移动没有稳定交给桌宠窗口。
- 修复：`DesktopPetWindow.paintEvent` 每次先透明清屏，再绘制气泡出生小球，减少透明窗口缓存残影。
- 修复：气泡状态下按住桌宠会先关闭气泡，再立即进入拖动状态，并通过 `grabMouse()` 抓住后续移动事件；释放时统一 `releaseMouse()` 并保存新位置。
- 验证：补充断言气泡关闭后进入 `ClosedHandCursor` 和鼠标抓取状态；相关桌宠气泡/拖动测试通过，`desktop_assistant/ui` 编译检查通过。
## 2026-05-04 全项目缺陷排查与稳健性修复

- 按验收前排查方式执行：完整编译检查、完整单测、模块导入级检查、运行日志尾部检查、桌宠素材链路检查。
- 发现工程问题：`python -m unittest discover` 默认只能显示 `0 tests ran`，原因是 `tests` 目录缺少包入口；新增 `tests/__init__.py`，现在默认 discover 可直接发现并运行测试。
- 发现桌宠运行稳健性问题：暮蝶素材优先依赖当前工作目录的 `runtime/pets/mudie`，如果未来从非项目根目录启动，可能回退到备用小球；新增项目根目录相对素材路径作为候选，降低快捷方式/打包入口导致素材丢失的风险。
- 补充测试：模拟切换当前工作目录后创建 `LivingOrb`，确认仍能加载项目内 `mudie` 桌宠素材。
- 验证结果：`python -m unittest discover -v` 当前为 `301 tests OK`；`python -m compileall desktop_assistant tests` 通过；198 个项目模块导入检查失败数为 0。
- 剩余风险：`desktop_pet_window.py` 与 `shell_window.py` 行数偏大，属于可维护性风险，不是当前回归失败点；后续适合单独做 UI 文件拆分，避免继续堆积。
## 2026-05-04 待办分类与双通道提醒

- 新增待办任务类型：`TodoTaskType.DAILY` 表示每日日常任务，`TodoTaskType.TEMPORARY` 表示临时任务；`TodoItem` 新增 `task_type`、`last_reminder_key`、`last_reminded_at`，用于分类展示和提醒去重。
- 待办 UI 新增分类选择：快速添加待办时可以选择“临时任务/每日日常”，详情编辑页也可以修改类型；待办列表会显示 `[临时任务]` 或 `[每日日常]`。
- 新增提醒判断模块 `desktop_assistant/todo/reminders.py`：临时任务按具体提醒时间只提醒一次；每日日常任务按“每天同一时间”每天提醒一次，避免每 30 秒重复弹窗。
- 主窗口新增提醒定时器：启动后约 1 秒检查一次，之后每 30 秒扫描到点的 open todo；触发后先写入提醒记录，再刷新首页。
- 提醒双通道：如果系统托盘可用，会调用 `QSystemTrayIcon.showMessage` 在右下角弹系统通知；如果常驻桌宠可用，会调用暮蝶桌宠的提醒气泡。
- 桌宠新增 `PetReminderBubble`：复用现有聊天气泡出生动画，从桌宠头部冒出提醒内容，自动收起；提醒气泡和右键菜单互斥，且气泡显示时仍可直接按住桌宠拖动。
- 验证结果：新增/更新相关测试通过；全量 `python -m unittest discover -v` 当前为 `306 tests OK`，`python -m compileall desktop_assistant tests` 通过。
## 2026-05-04 每日日常任务完成后从清单消失修复

- 根因：`每日日常` 之前复用了临时任务的完成语义，点击完成后会写成 `status=done`，而待办清单默认只显示 open，所以用户会感觉“每日任务没有出现/消失了”。
- 修复：新增 `daily_completed_on`，每日任务完成时只记录“今天已完成”，任务本体保持 open；临时任务仍然按原逻辑完成后隐藏。
- 修复：历史上已经被写成 `done` 的每日任务会在读取时自动规范化为可见的每日任务，并显示“今日已完成”。
- 修复：提醒系统会跳过“今日已完成”的每日任务，避免当天反复提醒；首页未完成统计也不再把今日已完成的每日任务算成待办压力。
- 修复：待办类型 UI 文案收敛到 `TODO_TASK_TYPE_ITEMS / TODO_DAILY_TASK / TODO_TEMPORARY_TASK`，减少硬编码。
- 验证：`python -m unittest tests.test_todo tests.test_ui_shell -v` 通过 82 项；`python -m compileall desktop_assistant tests` 通过。
## 2026-05-04 桌宠拖动动画

- 新增桌宠拖动专用覆盖状态：`LivingOrb.hold_drag_state()` / `release_drag_state()`，拖动动画不会永久覆盖执行中、等待、失败等业务状态。
- 拖动开始：桌宠先进入 `jumping`，表现为被拎起来的起势。
- 拖动中：根据鼠标横向移动方向切换 `running-right` / `running-left`，让桌宠跟随拖动方向动起来。
- 松手：释放拖动覆盖状态，播放一次 `jumping` 作为落地反馈，然后回到原来的基础/持有/一次性动作链路。
- 验证：新增拖动状态回归测试；`python -m unittest discover -s tests -v` 通过 310 项。

## 2026-05-04 提醒、待办语义、托盘与后台常驻产品化

- 提醒系统从“到点提示一次”推进到更接近产品闭环：支持稍后提醒 `snoozed_until`、重复提醒计数 `reminder_repeat_count`、错过提醒识别、默认静默时间 `23:00-07:00`，并按本机时区判断静默时间。
- 每日任务新增“今日跳过”语义：`daily_skipped_on` 只跳过当天，不删除任务、不改成完成；每日任务完成、跳过、稍后提醒都会清理对应提醒状态，避免第二天状态串扰。
- 临时任务和每日任务的提醒 key 继续区分：临时任务按具体时间去重；每日任务按日期和每日提醒时间去重，同时兼容旧版每日提醒 key，避免升级后重复弹。
- 提醒触发后会根据类型显示更明确的用户语言：普通提醒、错过提醒、重复提醒、稍后提醒；触达通道仍然是系统托盘右下角通知 + 暮蝶桌宠气泡。
- 待办详情页新增“今日跳过”按钮，仅对每日任务生效；临时任务点击时会给出“只适用于每日日常”的反馈，避免语义混淆。
- 系统托盘菜单补充“健康面板”和“刷新应用清单”：健康面板展示当前未完成待办、下一项任务、最近恢复提示和 capability debug 概览；刷新应用清单用于提升真实机器上的应用匹配命中率。
- UI 主入口新增单实例锁 `runtime/desktop_assistant_ui.lock`，启动时自动创建 `runtime` 目录；重复启动时第二个实例会直接退出，避免多个 `pythonw.exe -m desktop_assistant.ui` 同时常驻。
- 修正启动锁潜在运行时问题：`app.py` 补充 `Path` 导入，并确保锁文件目录存在，避免新机器或新工作目录首次启动时报错。
- 测试稳定化：托盘 + 桌宠提醒通道测试显式关闭静默时段，避免测试结果受当前真实时间影响；产品运行时默认仍然使用静默时段。
- 验证：`D:\anaconda3\envs\app\python.exe -m compileall desktop_assistant tests` 通过；`D:\anaconda3\envs\app\python.exe -m unittest tests.test_todo tests.test_ui_shell -v` 通过 86 项；`D:\anaconda3\envs\app\python.exe -m unittest discover -s tests -v` 通过 313 项。
## 2026-05-05 全局提醒设置与桌宠菜单入口

- 新增全局提醒设置：按“每日日常/临时任务 + 低/普通/高/紧急优先级”配置重复提醒间隔、最大重复次数、稍后提醒分钟数，并支持静默时段和每日任务重置小时。
- 待办清单页新增“提醒设置”入口；桌宠右键双击气泡菜单新增“提醒设置”选项，并移除气泡菜单里的“半透明/清晰”按钮。
- 提醒引擎接入全局设置：到点提醒、重复提醒、稍后提醒、静默时段、每日任务完成/跳过判断都走同一套设置。
- 修正每日任务 UI 状态：待办列表的“今日已完成”按全局每日重置小时判断，避免凌晨 4 点前显示和提醒逻辑不一致。
- 修复桌宠资源加载兼容性：Qt 当前环境不支持 WebP 时，自动 fallback 到 PNG spritesheet；已生成 `runtime/pets/mudie/spritesheet.png`，防止桌宠资源加载失败后回退成默认小球。
- 验证：`python -m compileall desktop_assistant tests` 通过；`tests.test_todo` 20 项通过；`tests.test_ui_shell` 拆分为 9 + 30 + 34 项全部通过。整类一次性运行在当前 Qt/测试清理环境下会超时，但拆分后 73 项均通过。
